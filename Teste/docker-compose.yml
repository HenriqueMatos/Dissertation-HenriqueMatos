version: "3.6"
services:
  keycloak:
    image: quay.io/keycloak/keycloak:16.1.0
    ports:
      - "8080:8080"
    environment:
      - KEYCLOAK_PASSWORD=admin
      - KEYCLOAK_USER=admin
      - PROXY_ADDRESS_FORWARDING=true
  rabbitmq:
    image: rabbitmq:3.9-management
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - ./rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf
      - ./rabbitmq.conf:/etc/rabbitmq/conf.d/10-default-guest-user.conf
      - ./definitions.json:/srv/definitions.json
      # - ./enabled_plugins.txt:/etc/rabbitmq/enabled_plugins